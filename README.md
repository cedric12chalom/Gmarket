# Gmarket

Plateforme marketplace de produits agricoles (marché TerraLink).

## Structure

- `backend/` — API Symfony (test-symfony)
- `frontend/` — Application web React + Vite (terralink-frontend-v2)
